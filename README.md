# grafana Provider

[embedmd]:# (grafana.tf)
```tf
provider "grafana" {
  version = "1.4.0"

  # auth - (required) is a type of string
  auth = null
  # url - (required) is a type of string
  url = null
}
```
